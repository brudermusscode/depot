<%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
<script type="text/javascript">
  I18n.defaultLocale = "<%= I18n.default_locale %>";
  I18n.locale = "<%= I18n.locale %>";
</script>
<%= javascript_pack_tag 'locale_switcher', 'data-turbolinks-track': 'reload' %>
<!DOCTYPE html>
<html>
  <head>
    <title>Depot</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons"
      rel="stylesheet">
    <%= javascript_importmap_tags %>
  </head>
  <body>
    <% if notice %>
      <div id="notifier" class="notifier">
        <div class="inr">
          <p><%= notice %></p>
        </div>
      </div>
    <% end %>
    <header class="main">
      <div class="inr">
        <div class="lt-cont">
          <ul class="menu">
            <a href="/">
              <li>Store</li>
            </a>
            <%= link_to products_path do %>
              <li>Products</li>
            <% end %>
          </ul>
        </div>
        <div class="rt-cont">
          <ul class="menu">
            <li>
              <aside>
                <%= form_tag store_index_path, class: 'locale' do %>
                  <%=
                  select_tag 'set_locale',
                  options_for_select(LANGUAGES, I18n.locale.to_s),
                  onchange: 'this.form.submit()'
                %>
                  <%= submit_tag 'submit', id: "submit_locale_change", style: "display:none;" %>
                <% end %>
              </aside>
            </li>
            <% if session[:user_id] %>
              <%= link_to orders_path do %>
                <li>Orders</li>
              <% end %>
              <%= link_to users_path do %>
                <li>Profile</li>
              <% end %>
              <%= link_to carts_path do %>
                <li><span style="font-size:24px;">🛍️</span></li>
              <% end %>
              <%= link_to logout_path, { method: :delete } do %>
                <li><span style="font-size:24px;">➡️</span></li>
              <% end %>
            <% end %>
          </ul>
        </div>
      </div>
    </header>
    <flex-layout>
      <nav class="side_nav">
        <% if @cart %>
          <div id="cart" class="carts">
            <%= render_if @cart && @cart.line_items.any?, @cart %>
          </div>
        <% end %>
        <% if @product && @product.id.present? %>
          <div class="product">
            <%= image_tag @product.image_url, style: 'width:92%;border-radius:6px;border:1px solid rgba(0,0,0,.12);', class: '' %>
          </div>
        <% end %>
      </nav>
      <div class="content">
        <main class='<%= controller.controller_name %>'>
          <%= yield %>
        </main>
      </div>
    </flex-layout>
  </body>
</html>

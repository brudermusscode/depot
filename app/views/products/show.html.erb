<%= render "toolbar", product: @product %>
<%= render @product %>
<!-- review/comment section -->
<div style="width:80%;margin:24px auto;border-top:1px solid rgba(0,0,0,.12);"></div>
<%= render @product.reviews %>
<%= render 'reviews/form', product: @product %>
